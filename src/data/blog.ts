import type { BlogPost } from "@/lib/blog";

// Empty array as legacy data has been migrated to markdown files in src/content/blog
// This file is kept for type compatibility if needed, but should be empty of heavy content.
export const blogPosts: BlogPost[] = [];
